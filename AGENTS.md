# AGENTS.md — 小树游戏屋协作指南

本文件适用于整个仓库。任何人或自动化 agent 在修改代码、测试、文档、构建或部署前，
都应先完整阅读本文件。涉及线上服务器、nginx、DNS、证书或发布时，还必须完整阅读
[`docs/ops.md`](docs/ops.md)。

## 0. Agent 执行约束（包括 Zcode）

- 开工先读规则、检查 `git status --short` 和本批 diff；用一段简短说明列出本批目标、
  修改范围、不变量及按 §10 选择的验收。已有授权内直接执行，不为例行操作反复询问。
- 每批只处理一个可独立验收的目标。不得借 UI 升级改规则、存档、依赖或重写无关模块。
  发现范围外问题先记录位置、影响与建议，不顺手修；必须扩展范围才能完成目标时说明原因。
- 共享组件先在一个代表游戏完成真实操作验收，再推广；不同输入/尺寸/生命周期的接入方
  必须各有代表。不能把一次全站批量替换当作已经完成集成。
- 修复先复现；规则缺陷先补失败测试。只测外部行为和重要不变量，不为颜色、文案或
  可逆样式改动编写与实现逐行对应的测试。
- 禁止通过删断言、skip、隐藏异常、预置状态绕过本批变更、扩大超时/缓存限制来掩盖失败。
  确需调整测试或预算，先说明旧标准为何失效，并保留等价或更强的行为证据。
- 交付前复查 diff，核对本批目标、意外改动、触控/存档/销毁等受影响不变量。
  不得用自己的“完成总结”替代实际命令、退出码和操作证据。
- 测试只按 §10 的触发条件运行；不得每改一行跑全量、并行启动重复回归、无理由重跑已通过项。
- 实测通过、失败、未测必须分开写；截图必须打开检查，文件生成不等于视觉通过。
  禁止声称未运行的 Safari 真机、离线、音频听感或全游戏验收通过。
- 默认完成本地提交；推送与发布授权统一遵循 §12–13。文档不是权限隔离机制，
  不能宣称本规则已自动配置 CI、远端分支保护或部署凭据隔离。

## 1. 项目目标

这是给家中孩子在 iPad 上玩的无广告小游戏合集，产品名为「小树游戏屋」。应用是纯静态
PWA，没有后端、账号、付费、广告或数据采集，联网一次后应能离线运行。

所有决策按以下优先级取舍：

1. 孩子能独立、安全、直观地玩；
2. iPad Safari 的触控体验可靠；
3. 无广告、无追踪、无不必要的外部网络依赖；
4. 规则正确，动画和声音清楚但不过度刺激；
5. Mac 浏览器可用键盘、触摸板或鼠标辅助测试；
6. 核心规则可测试，界面代码易于替换。

除非用户明确要求，否则不得加入广告 SDK、统计 SDK、登录、排行榜服务、远程字体、远程
图片、远程音频或其他运行时第三方请求。新增依赖前先说明必要性，优先使用已有能力。

## 2. 当前产品范围

| 路由 | 游戏 | 主要操作与特性 |
| --- | --- | --- |
| `#/2048` | 2048 | 触摸滑动、方向键、IndexedDB 自动存档 |
| `#/gomoku` | 五子棋 | 默认人机，也支持同屏双人 |
| `#/tetris` | 俄罗斯方块 | 屏幕按钮和键盘、七袋随机、幽灵落点、等级系统 |
| `#/merge-fruit` | 合成水果 | Matter 圆形物理、连锁合成、下一个预告、危险线 |
| `#/freecell` | 空当接龙 | 拖牌或点选移动、四个空当、多牌组移动、花色回收区、残局自动收完 |
| `#/spider` | 蜘蛛纸牌 | 一花色入门模式、拖序列或点选、两副牌、五轮发牌、自动收组 |
| `#/minesweeper` | 扫雷 | 9×9/16×16、首点安全、触控插旗、右键插旗、数字展开 |
| `#/memory` | 记忆翻牌 | 12/16/24 对、水果/动物、单人/双人轮流 |
| `#/tangram` | 七巧板 | 标准七块、五幅轮廓含房子/帆船、剪影多解判定、拖动吸附、旋转、翻面 |
| `#/pipes` | 接水管 | 4×4/5×5/6×6 可解管网、旋转、撤销、提示 |
| `#/sokoban` | 推箱子 | 30 个可解关卡（选关页列全部关卡，挑战关带 ☆ 标记）、按步数评星、点相邻格/方向按钮/键盘、撤销、提示 |
| `#/bubbles` | 泡泡龙 | 四色基础/五色挑战、反弹瞄准线、三连消除、悬空消除、交换、撤销 |
| `#/pop-bubbles` | 点泡泡 | 泡泡上升渐大、点破计分（越小越高）、金色三倍、限时、基础/进阶/挑战并渐进加速 |
| `#/red-rain` | 红包雨 | 红包落下点开收福、连击加倍、炮仗扣分、限时、基础/进阶/挑战并渐进加速 |
| `#/whack-mole` | 打地鼠 | 九洞地鼠随机冒头、连击加倍、睡帽鼠宝宝不能打、限时三档 |
| `#/fruit-slicer` | 切水果 | 抛物线水果、滑动刀光切割、一刀多果奖励、炸弹扣时、限时三档 |
| `#/untangle` | 解绳结 | 6-9 个点、拖动、交叉线高亮、撤销、提示 |
| `#/maze` | 迷宫探险 | 7×7 至 11×11 可解迷宫、点相邻格/按住拖动/方向按钮/键盘 |
| `#/nonogram` | 数织 | 50 幅唯一解图案（原图 0-8 身份稳定，变体追加）、从第 1 幅开始循环全量、连续划格、整笔撤销、线索完成提示、续玩 |
| `#/sudoku` | 数独 | 6×6/9×9 唯一解题、候选数笔记、同数/冲突高亮、撤销、续玩 |
| `#/water-sort` | 水排序 | 试管倒水归色、3/5/6 色三档、反向构造生成必可解、撤销、续玩 |
| `#/parking` | 停车场 | 6×6 滑车解谜、单指直接拖车（轻点出目标格作辅助）、红车开出出口、BFS 步数带生成、撤销、续玩 |
| `#/tile-match` | 叠叠消 | 多层错位叠放、7 格槽三同消除、槽满不清盘给免费撤销/洗牌、生成贪心验证可解 |

原有游戏的列表与路由在 `src/app/app.ts`；新增益智游戏由 `src/app/puzzles.ts` 注册名称、
路由 ID 和动态 import，`app.ts` 统一生成卡片并处理导航。添加、删除或改名游戏时，必须
同时检查大厅卡片、点击事件、动态 import、hash 路由和 `README.md`。
大厅分类和本地最近记录在 `src/app/library.ts`；新增游戏时也应指定分类。

## 3. 技术栈与运行方式

- TypeScript 7，开启严格模式、`noUncheckedIndexedAccess`、未使用代码检查；
- Vite 8；
- Phaser 4.2，负责 Canvas/WebGL、场景、输入、时间轴和动画；
- Phaser Matter，仅合成水果使用；
- Vitest 5，Node 环境下测试纯规则；
- vite-plugin-pwa + Workbox，`registerType: 'prompt'`；
- pnpm 10，版本声明见 `package.json#packageManager`。

常用命令：

```bash
pnpm install
pnpm dev --host
pnpm test
pnpm test:watch
pnpm test:browser # 先启动开发服务，默认使用本机 macOS Chrome
pnpm test:browser:ui # 同上；新用户帮助/输入隔离/棋盘缩放专项
pnpm check
pnpm preview
```

`pnpm check` 依次执行 TypeScript 检查、全部单测和生产构建。它是高影响变更与发布的
批次验收命令，不是每次编辑或每个提交的默认动作；具体选择见 §10。

## 4. 目录职责

```text
.
├── src/
│   ├── app/                     # 大厅、hash 路由、全局页面样式
│   ├── games/
│   │   ├── cards/               # 纸牌模型、洗牌及共享纸牌绘制
│   │   ├── puzzle-kit/          # 新增益智游戏共用的触控场景与装配
│   │   ├── action-kit/          # 动作游戏共用的逐帧循环、限时回合与结算面板
│   │   └── <game>/
│   │       ├── core/            # 纯规则和状态转换，不依赖 Phaser/DOM
│   │       ├── index.ts         # Phaser 实例装配和 destroy 生命周期
│   │       └── scene.ts         # 绘制、输入、音效和场景调度
│   ├── platform/
│   │   ├── audio/               # WebAudio 程序化音乐与音效
│   │   ├── display/             # 游戏宿主视口测量
│   │   └── storage/             # IndexedDB 存档封装
│   └── main.ts                  # 应用启动与 PWA 更新事件
├── tests/<game>/                # 与 core 对应的规则测试
├── public/                      # favicon 与 PWA 图标
├── docs/ops.md                  # 线上发布和服务器运维手册
├── vite.config.ts               # Vite、Vitest、manifest、Workbox
└── package.json                 # 开发、检查、构建与部署入口
```

`dist/`、`node_modules/`、测试报告均为生成物，不提交。不要手改 `dist/`。

## 5. 架构约束

### 5.1 规则层

- `core` 必须是确定性的状态转换：输入状态和动作，返回新状态或明确结果；
- 不得访问 Phaser、DOM、Canvas、AudioContext、localStorage 或 IndexedDB；
- 随机行为通过 `RandomSource = () => number` 注入，测试中使用固定随机源；
- 不直接修改调用方持有的状态；优先返回新对象/数组；
- 非法动作不得悄悄破坏状态，应返回 `changed/moved: false` 或抛出明确参数错误；
- 新增规则或修复规则缺陷时，先补能复现问题的测试，再修改实现。

共享纸牌逻辑放在 `src/games/cards/core/cards.ts`，不要在空当接龙和蜘蛛纸牌中分别复制牌面、
花色或洗牌实现。

### 5.2 显示层

- `scene.ts` 只负责把 core 状态映射为画面、接收输入、安排动画和播放声音；
- `index.ts` 创建 Phaser 游戏并返回 `{ destroy() }`；`destroy` 必须销毁 Phaser 实例、音频和
  本游戏注册的外部资源；
- 离开路由时，大厅会调用当前游戏的 `destroy()`。不得让 timer、监听器、AudioContext 或
  Matter 世界在离开后继续运行；
- Phaser 停止场景触发 `SHUTDOWN`，销毁整个游戏可直接触发 `DESTROY`。外部监听器和
  异步存活标记的清理必须覆盖两者且幂等，不能只监听 `SHUTDOWN`。
- `patches/phaser@4.2.1.patch` 修复框架可见性/焦点监听器销毁遗漏，由
  `pnpm-workspace.yaml` 自动应用；升级 Phaser 前先确认上游是否已修复，并保留相关回归测试。
  修改依赖补丁后，开发服务使用 `pnpm dev --force` 重新预构建，避免测试到旧的 Vite 依赖缓存。
- Phaser 场景内使用逻辑坐标，依靠 `Phaser.Scale.FIT` 和 `CENTER_BOTH` 适配屏幕；
- 全部游戏经 `enableHighDpi`（`src/platform/display/high-dpi.ts`）按设备像素密度渲染，
  DPR 上限 2、总画布像素 ≤4M；逻辑坐标与触控面积不变。这是对 Phaser 4.2.1 的定点
  适配（无官方 resolution 配置），升级 Phaser 前必须用 `RENDER_ONLY=1 pnpm test:browser`
  做 DPR=2 回归。
- 空当接龙/蜘蛛纸牌使用横屏逻辑尺寸 `1024×768`；合成水果/扫雷使用竖屏 `768×1024`；
  其他游戏当前随容器尺寸启动。修改方向或尺寸前要同时验证 iPad 横竖屏。
- 固定比例游戏在反方向仍须完整可玩，并在画布留白区显示非阻塞的方向建议；不得强制旋转或
  因设备旋转重开棋局；
- 益智/动作游戏共用 portrait-fluid 挂载（`puzzle-kit/mount.ts`）：逻辑宽固定
  `768`，竖屏逻辑高 `clamp(768×宿主高/宿主宽, 900, 1680)`，横屏回退 `768×900` FIT；
  玩法坐标仍按旧 `768×900` 编写，由内容层整体纵移与 `legacyPoint(pointer)` 换算，
  不得在各游戏里手写 pointer 原始坐标与 core 比较；按钮逻辑高度为 60px；
  顶栏统一走 `createPuzzleChrome`（应用级图标不随内容层平移）。
  规则位于各自 `core`，共享场景只处理显示、输入入口、声音和生命周期。
- 数独选格和数织划格必须复用棋盘对象，只在换尺寸/重建页面时创建布局；不要恢复为每次输入
  销毁重建所有文字。数织同一笔插值补齐经过的格子，只增加一个撤销状态。
- `RESIZE` 游戏的 Canvas 显示尺寸必须跟随 `.game-host`。Phaser 复用 Canvas 时可能保留上一
  个 `FIT` 游戏的内联尺寸，不能移除 `data-preferred-orientation="any"` 对应的 CSS 覆盖。

### 5.3 路由与异步装载

- 路由使用 `window.location.hash`，不依赖服务端 history fallback；
- 游戏通过动态 import 拆包，避免大厅首屏加载全部场景；
- `navigationId` 用于丢弃过期的异步装载结果，新增路由时保持这一防竞态模式；
- 游戏内退出统一调用大厅的 `goHome()`，通过应用自己的 history state 回到首页；
- 首页保留退出边界，回退时必须先显示确认，不能直接退回浏览器历史；
- 不要绕过 `openGame()` / `goHome()` 直接写 hash，否则会破坏回退栈。

## 6. 交互、动画与声音

### 6.1 iPad 优先

- 所有核心操作必须能用单指完成；不能要求 hover、精确右键或键盘；
- 交互目标应足够大，避免把重要按钮放进 Safari 边缘手势区域；
- 桌面键鼠是补充：例如 2048 使用方向键、扫雷右键插旗，但触控必须有等价入口；
- 禁止页面意外滚动、双击缩放或在 Canvas 与浏览器手势之间产生明显冲突；
- 文案使用简体中文，短、明确、适合孩子理解。

### 6.2 动画原则

- 动画只作用于发生变化的对象，不能用“全盘刷新动画”冒充反馈；
- 2048 新方块从格子中心以 50% 尺寸出现，再撑满格子；
- 2048 合成方块以中心为原点放大约 20%，再恢复；未变化方块不播放动画；
- 其他游戏也遵循“动作对象有反馈、静止对象不乱动”，动画不得妨碍连续操作；
- 合成水果的显示半径、Matter 碰撞半径和规则配置半径必须一致；Matter `enableSleeping`
  必须保持关闭，否则支撑水果合并消失后可能出现悬空刚体。

### 6.3 音频原则

- 音乐和音效由 `GameAudio` 使用 WebAudio 程序化生成，不依赖联网素材；
- Safari 要求用户手势解锁音频。每个场景首次 `pointerdown`/键盘操作应调用 `audio.unlock()`；
- 所有场景共享 `family-game-room-muted` 静音偏好；
- 必须提供明显的开关，静音时不得播放操作、胜利或失败音效；
- `destroy()` 时调用 `audio.dispose()`，避免离开游戏后音乐继续。

## 7. 状态和存储

- IndexedDB 数据库：`family-game-room`，object store：`game-saves`；
- 2048 存档键：`game-2048-v1`；改变持久化结构时使用新版本键或提供兼容迁移；
- 益智游戏进度键：`game-puzzle-progress-v1`，结构为 `{ best, flags, levels }`（最好成绩、
  完成标记、关卡进度），由 `src/games/puzzle-kit/progress.ts` 统一读写，大厅卡片异步
  显示成绩摘要；结构变更时换新版本键并提供兼容迁移；
- 静音偏好：localStorage `family-game-room-muted`；
- 俄罗斯方块与合成水果分别在 localStorage 保存最高分；修改键名时保留旧数据兼容；
- 四款动作游戏按难度分档存最高分：localStorage `family-game-room-<id>-v2-best-<mode>`，
  进度键 `<id>:v2:<mode>`；节奏调整时换版本键（旧键成绩保留显示「旧节奏」不冒充新档）；
- 存储不可用时游戏必须仍可开始，封装应捕获隐私模式或配额错误；
- 不存储个人信息，不上传游戏数据。
- 数独/数织/推箱子/水排序/停车场棋盘存档键为 `game-puzzle-draft-<id>-v1`；包含版本、更新时间、棋盘、
  最近 50 步撤销和是否使用提示，数独包含候选笔记（旧存档缺笔记时补空数组）。
  `draft-storage.ts` 合并短时间内的写入并串行落盘，切后台/离开时 flush；localStorage 同键
  保存应急备份，读取时校验并择新，不能用异步旧结果覆盖新操作。损坏数据不得阻止开局。
- 其他新增益智游戏只保存成绩/完成记录，不保存棋盘；最近 4 款游戏存在 localStorage
  `family-game-room-recent-v1`，必须过滤未知 ID 并容忍存储不可用。
- 小树成长：localStorage `family-game-room-tree-v1`（叶子数与已发里程碑），每个自然目标只发一片、叶子只增不减、坏档容忍；由 `recordFlag` 与动作游戏新纪录统一发叶，大厅 hero 右上渲染阶段徽章。
- 大厅图标顺序：localStorage `family-game-room-order-v1`（游戏 ID 数组），读取时过滤未知
  ID、新游戏按默认顺序补在后面；「整理图标」编辑模式拖拽排序时写入，「恢复默认」删除。
- 记忆翻牌分 `single/duo`，泡泡龙分颜色数；接水管、解绳结、迷宫和推箱子成绩键分
  `:solo/:assisted`。提示标记不可通过撤销洗掉。旧成绩不冒充新模式的独立成绩。
  迷宫新记录为超出最短路线的步数，推箱子仅独立完成评星，提示练习标勾。

## 8. 各游戏不可破坏的规则

- 2048：一次滑动按正确方向压缩/合并，每个方块每回合最多合并一次；有效移动才生成新块；
- 五子棋：落子边界、占位检查、连续五子判胜必须由 core 负责；电脑选择支持固定随机测试；
- 俄罗斯方块：七袋、碰撞、旋转、锁定、消行、幽灵落点均保持可测试；
- 合成水果：仅同等级接触才升级；最大水果不再升级；水果必须持续受重力影响；下一颗预告
  在投放后正确前移；杯体当前宽高比约 `0.80`；樱桃与草莓不能只靠近似颜色区分；
- 空当接龙：牌列红黑交替递减；多牌移动上限由空当和空列共同决定；回收区同花色递增；
  各列从底到顶 rank 严格递减 1 时进入自动收尾（canAutoFinish/nextAutoMove），逐步回收到
  胜利，期间忽略走子输入；
- 蜘蛛纸牌：当前是一花色入门版；只能移动连续递减序列；发牌前不得存在空列；K 到 A 自动收组；
- 扫雷：首次揭开及其邻格无雷；空白连片展开；插旗不等于揭开；数字展开要求邻旗数匹配；
  胜负后不再接受棋盘动作。
- 记忆翻牌：翻错两张后的展示期间不能翻第三张；双人翻错换手，配对成功保留当前玩家。
- 七巧板：标准两大、两小、一中三角形、一正方形、一平行四边形；几何位置和方向均符合
  目标才吸附，翻面也参与判定；剪影以凸多边形相交面积检查覆盖和重叠，允许同形块互换与
  其他合法拼法；不能只按中心距离判定成功。切模式重开图案；提示参考拼法可撤销。
- 接水管：管口必须双向对应；所有格子与左上水源连通才成功，随机生成始于可解树结构。
- 推箱子：只推一箱、不能穿墙或推两箱；内置关卡必须通过求解测试，PAR 星级阈值
  与求解器最优步数保持一致（有测试校验）。第 11-30 关由固定种子（mulberry32 20260919）
  随机生成并经同预算 BFS 验证，生成方式见 core 注释。
- 泡泡龙：六邻格、同色连通至少三颗消除；失去顶部支撑的泡泡一并消除；颜色从剩余棋盘选取。
- 点泡泡：泡泡持续上升变大，越小分越高；金色泡泡三倍；到顶消失不惩罚，时间到结算。
- 红包雨：连击窗口 800ms，3 连起两倍、6 连三倍封顶；炮仗扣 3 分并断连击，分数不倒扣为负。
- 打地鼠：地鼠只在空穴生成且按时缩回；普通地鼠按连击倍数计分；睡帽鼠宝宝扣分；打空断连击。
- 切水果：切割判定用滑动线段与水果圆求交，不逐帧命中；一次连续挥刀跨多个输入事件累计，
  切中 3 果时奖励 +3，一次挥刀只奖励一次，抬手/离开游戏区域清零连切；炸弹扣 5 秒
  不清分数；漏接不惩罚。
- 解绳结：无关边的交叉、接触和重叠均需处理；点不能重叠冒充解开。
- 迷宫：移动必须遵循通道，生成器保证出口可达；按住拖动按线段逐格插值前进，碰墙或斜穿
  角落立即停止；旋转屏幕不重开迷宫。
- 数织：行列线索对应唯一解（50 幅图案：9 幅手工原图 + 镜像/转置/反色双射变体，均经求解器
  逐一验证；原图 0-8 身份稳定、变体追加在后）。三代编号存档兼容：扩容前与当前同身份直取、
  已发布交错版经精确双射映射，无法区分的浅进度按原身份保守恢复并一次性备份原档（-backup 键）；
  完成标记 nonogram-N 的 N≥9 段自动迁移、小树里程碑继承不重发。多填格不能判胜，不强制把所有空格标叉。
- 数独：生成器挖空后必须仍唯一解；题面格不可修改；填满且行/列/宫无冲突才判胜，
  允许先填错值并用红色高亮冲突。
- 水排序：顶段只能倒入空管或同色顶并受容量截断；生成从完成态反向打散并用贪心
  复核可解，初始不允许已完成的单色满管。旧 v1 四色进阶/五色挑战按存档颜色数
  原样续玩（含撤销历史）；新开局才切换到当前颜色数。
- 停车场：车辆只能沿自身朝向滑到畅通位置；主车滑到出口位即胜；生成关卡必须
  BFS 可解且步数在难度带内；随机出题最多 8 轮、每轮 2000 节点，预算耗尽使用
  BFS 验证的同档备用关卡（5/8/17 步），不得降为两步局。
- 叠叠消：被上层压住的方块不可拾取；同款凑三消除；槽满不清盘，撤销与洗牌免费
  且洗后必须重新验证可解。洗牌将槽中尚未消除的牌放回原位置，再洗剩余花色；
  已消除组保留，成功后清空旧撤销历史，失败不改棋盘、不扣次数。可解验证必须遵守满槽禁取。

改变上述玩法属于产品行为变更，必须在交付说明中明确指出，不能作为顺手重构悄悄修改。

## 9. 新增游戏的标准流程

1. 明确规则、输入方式、胜负条件、逻辑尺寸和是否需要存档；
2. 创建 `src/games/<id>/core/game.ts`，先实现纯规则；
3. 创建 `tests/<id>/game.test.ts`，覆盖正常流程、边界、非法动作、胜负和固定随机；
4. 单独提交“规则 + 测试”；
5. 创建 `scene.ts` 与 `index.ts`，接入触控、键盘补充、音频和销毁生命周期；
6. 在 `src/app/app.ts` 加大厅卡片、点击处理、动态 import 和 hash 路由；
7. 更新 `README.md` 的可玩列表；必要时更新本文件；
8. 分别在 iPad 竖屏/横屏尺寸和 Mac 浏览器验证；
9. 运行 `pnpm check`，再单独提交“可玩界面”。

优先保持这种分批历史，便于规则回归和界面回退。不要把多个游戏或无关重构塞进一个提交。

## 10. 测试与验收

### 10.1 按影响范围选择检查

先看整个批次 diff 和实际调用方，不能只按最后一个修改文件判断。多类改动取要求并集。
所有批次提交前都执行 `git diff --check`；以下是本地提交/交付的最低要求：

| 改动范围 | 必须执行 | 不默认执行 |
| --- | --- | --- |
| 纯文档、注释、审查报告 | 核对事实、命令和本地链接；diff 检查 | 类型检查、单测、构建、浏览器 |
| 单游戏纯规则/core | 类型检查 + 该游戏全部单测；新增/修复规则先有失败测试 | 其他游戏单测、浏览器；改变用户玩法时另加目标游戏操作验收 |
| 单游戏 scene/index、文案、样式 | 类型检查（改 TS 时）+ 目标游戏浏览器专项；涉及规则才补该游戏单测 | 全量单测、全量浏览器 |
| 单游戏素材、预加载、import、路由注册 | 类型检查（改 TS 时）+ 一次生产构建 + 目标入口/资源专项；素材涉及离线时验离线 | 无关游戏单测与全量截图 |
| 共享规则、cards/core、存档格式/迁移、共享进度 | 批次收尾一次 `pnpm check`；存档变化加旧档/坏档/续玩专项 | 每次编辑都跑 check；无关视觉截图 |
| 共享颜色/token、文案、纯装饰样式（不改布局/输入/生命周期） | 类型检查（改 TS 时）+ 受影响代表页面的视觉专项 | 全量单测、构建、完整浏览器回归 |
| 共享 UI 行为/布局、puzzle-kit/action-kit、全局布局、音频、输入、显示适配 | 批次收尾一次 `pnpm check` + 受影响接入类型的浏览器专项 | 逐游戏重复相同测试 |
| 路由/返回栈、共享挂载/销毁、全局事件、跨游戏生命周期 | 一次 `pnpm check` + 一次完整 `pnpm test:browser` + 变更专项 | 同一版本重复完整回归 |
| package/锁文件、Phaser 补丁、构建/TS/Vitest 配置 | 一次 `pnpm check`；运行时依赖/框架变化再跑完整浏览器，Phaser/DPR 变化另做 DPR=2 专项 | 仅测试工具变化时跑无关游戏操作 |
| SW、manifest、缓存策略、PWA 启动/更新 | 一次 `pnpm check` + 生产 preview 的安装/更新/离线专项 | 用开发服务器代替生产验证 |
| 测试脚本、断言 | 运行被修改的测试路径，确认能抓住目标失败；仅测试改动不要求构建游戏 | 为证明脚本语法正确而全量重测产品 |

可直接使用现有命令，无需新增依赖或测试包装器：

```bash
pnpm exec tsc --noEmit
pnpm exec vitest run tests/gomoku/game.test.ts
pnpm exec vitest run tests/freecell tests/spider tests/klondike
pnpm exec vitest run tests/platform/draft-storage.test.ts tests/platform/puzzle-drafts.test.ts
pnpm build       # 已含类型检查，此次无需再单独跑 tsc
pnpm check       # 已含类型、全部单测和构建，不再逐个重复执行
```

定向单测使用 `pnpm exec vitest run <真实路径>`，不加 `--passWithNoTests`；必须确认实际
运行了预期文件和非零测试。路径错误、零测试、脚本提前退出均不能算通过。

**执行时机与结果复用：**

- 开发中跑复现用例和目标测试，稳定后才做表中批次验收。不因中间提交重复全量检查。
- 记录测试对应的提交号；未提交状态记录基线 SHA、修改文件和测试后是否又改过代码。
  同一相关代码、依赖、配置、测试与运行环境下的通过结果可复用；仅补文档或提交本身不使结果失效。
- 测试后再改代码，重跑受影响项；若改到共享层或影响面扩大，按表升级。
  发布要求覆盖最终产物的有效全量结果；最后一次全量后有运行时代码改动则发布前重跑。
- 失败先定位，修复后重跑失败项及受影响项。禁止无分析地重复全量碰运气；修测试脚本时
  同时确认失败不是被吞掉。无法界定影响范围时说明原因，再升级为全量。
- 新增游戏按 §9 收尾验收；用户明确要求全量时执行全量。其余情况不得把全量当保险动作。
- 测试数量以实际运行结果为准，不在此维护易过期的固定数字，也不得为维持数量删减覆盖。

### 10.2 浏览器验收

专项只打开受影响游戏和相关状态，不等于执行全套 `pnpm test:browser`。
当前完整脚本没有单游戏过滤参数，不得虚构 `--game` 等选项；可以用独立 CDP 专项或人工
操作，并记录具体步骤、断言与证据。可重复的缺陷回归应把脚本保存在仓库，不能只留下
“另有临时脚本验过”的注释。共享改动按实际引用列出代表游戏和选择理由。

涉及场景、布局、动画、输入、PWA 或音频时，至少完成受影响项：

- 用本地开发服务打开目标 hash 路由，确认无运行时错误；
- 用 `768×1024` 检查 iPad 竖屏，用 `1024×768` 检查横屏；响应式/移动布局另测 `390×844`；
- 实际执行核心操作，而不只是截取初始画面；
- 检查文字不重叠、按钮不越界、小元素可辨认、结果面板不遮挡必要操作；
- 音频必须通过真实用户手势测试解锁、静音、恢复；
- 物理类游戏要连续投放并触发合成，观察静止、滚动、碰撞和支撑消失后的行为；
- 帮助/弹窗改动必须覆盖首次进入、手动打开、关闭、重开和旋转；弹窗显示期间触控与
  键盘不能操作棋盘，关闭手势不穿透，计时状态准确恢复；连续开关后监听器回到基线；
- 缩放/拖动改动必须验证操作后的顶栏与命中位置，不能只验初始画面；
- 变更 Service Worker 时，用生产构建/preview 检查更新流程和离线加载。生产测试不得
  依赖 `/src/`、开发专用 import 或调试场景句柄；失败必须报告，不能标为已验离线。

已知阻断已修复（2026-09-19）：`PRODUCTION_ONLY=1 pnpm test:browser` 现由 Node 侧读取
帮助清单注入旧用户 fixture，不再从浏览器请求 `/src/`（Network 门禁发现即失败），并在
生产 preview 上真实操作首弹/手动帮助与离线重开，已完整通过。新用户帮助、输入隔离与
监听器基线由 `pnpm test:browser:ui`（`scripts/browser-ui-regression.mjs`，dev 环境）覆盖。
背景见 [审查报告](docs/reviews/2026-09-19-product-review.md)。

可以使用本机 Chrome headless 截图进行视觉回归，但截图不能替代输入和动态行为验收。
`pnpm test:browser` 使用独立临时 Chrome 配置执行触控、键鼠、音频状态、续玩、返回边界、
高难度和横竖屏回归，并输出截图目录；它不是 Safari 真机或人耳听感验证的替代品。

### 10.3 调试进程清理纪律

本机起的调试/验收进程用完必须关闭，不得留到用户发现（2026-09-19 用户明确要求固化为规则）：

- 涵盖范围：CDP 无头 Chrome、`pnpm dev` / `pnpm preview`、自己起的构建监听、临时端口
  监听等一切为本任务启动的本机进程；
- 收尾时按 PID / 进程树逐个 kill 自己起的实例，再用 `lsof -nP -iTCP:<端口> -sTCP:LISTEN`
  确认端口已释放、`ps` 确认无残留；
- 测试用 Chrome 必须以独立临时 `--user-data-dir` + `--remote-debugging-port` 启动，与
  用户自用 Chrome 严格隔离；严禁按进程名批量 kill（会误杀用户正在用的浏览器）；
- 批次中途确需常驻的（如回归反复用的 `pnpm dev`），必须在交付说明里写明留下的进程、
  端口与用途；批次交付后仍存活的一律收掉。

## 11. PWA 与更新机制

- `vite.config.ts` 中 `base: './'` 必须保留，以支持静态目录部署；
- manifest、图标和 Workbox 预缓存范围由 `vite.config.ts` 管理；
- Service Worker 使用 prompt 更新：发现新版本后派发 `app-update-available`；游戏中只暂存
  更新，回到大厅后再提示，禁止在游戏中途强制刷新；
- 大厅支持手动检查更新；应用启动、恢复前台、重新联网和每小时也会检查一次。应用更新重载
  后应显示完成提示；
- 新增本地静态资源时确认其扩展名包含在 `workbox.globPatterns`，否则离线可能缺失；
- 修改 PWA 名称、颜色、方向或图标后，需要同时检查 Safari“添加到主屏幕”的结果；
- 不要用普通 HTTP 真机验证 PWA 安装；线上 HTTPS 是最终准绳。

## 12. Git 工作规范

- 当前主分支为 `main`；尊重已有未提交修改，不覆盖、不清理用户或其他 agent 的工作；
- 用户要求用 Git 记录历史：实现完成并验证后要提交；
- **默认工作流为「按影响验证 → 本地提交 → 报告」**。本节取代历史上默认自动推送部署的
  约定；旧运维记录仅描述历史，不构成当前授权。
- 用户明确要求推送时才推送；明确要求发布/部署时才按 §13 部署。推送不自动授权部署，
  部署也不自动授权推送；用户已对本批明确授权的动作不重复询问。
- 禁止 `git reset --hard`、强制 checkout、rebase 已共享历史或其他破坏性操作；
- 提交应小而完整，规则、界面、修复、文档、部署配置尽量分开；
- 标题使用有信息量的 Conventional Commit 风格，例如：
  `feat: add playable minesweeper game`、`fix: keep unsupported fruits falling`；
- 提交正文建议写明：为什么改、改了什么、如何验证；禁止 `fix bug`、`update` 等空泛说明；
- 提交前检查 `git status --short` 和 `git diff --check`，提交后确认工作区符合预期。

## 13. 发布与线上运维

线上地址：<https://games.dreamser.com/>。

**发布是外部状态变更，不能因为代码已完成就自动执行。只有用户明确要求部署/发版时才能运行：**

```bash
pnpm run deploy
```

当前命令等价于本地构建后，通过 SSH 端口 1991 将 `dist/*` 上传至
`ubuntu@101.42.50.27:/home/www/games`。它不会自动删除服务器上的旧 hash 资源；旧文件通常
不影响运行，但不得擅自用递归删除清理线上目录。

发布前后最低流程：

1. 完整阅读 `docs/ops.md`；
2. 确认工作区、待发布提交和授权范围；
3. 确认最终待发布代码已有有效的 `pnpm check` 全量结果及本批专项证据（复用条件见 §10.1）；
4. 用户明确授权后运行 `pnpm run deploy`；
5. 只读验证 HTTP→HTTPS、HTTPS 200、证书、入口页、manifest、`sw.js` 和缓存头；
6. 对比线上入口/SW 与本地 `dist`，确认实际发布版本；
7. 在支持时验证主屏幕 PWA 更新，但不要在游戏进行中强制刷新。

### 13.1 服务器铁律

- 本地仓库是源码唯一真相；服务器 `/home/www/games` 只存放构建产物，严禁直接在线改项目；
- nginx/证书操作必须先备份，再修改，随后 `sudo nginx -t`，通过后才平滑 reload；
- 同机运行多个其他站点，不得修改无关 server block、进程或目录；
- 不要通过 apt 安装或卸载 certbot。当前 certbot 位于 `/opt/certbot` venv，apt 操作可能
  mask 自动续期 timer；
- `sw.js`、`manifest.webmanifest`、`index.html` 的 no-cache 策略是 PWA 更新链路的生命线；
- 证书 HTTP-01 自动续期依赖 DNS 指向正确、80 端口可达以及
  `/.well-known/acme-challenge/` location；
- 任何 nginx、DNS、证书、防火墙或服务器排障都以 `docs/ops.md` 为详细事实来源，并在变更后
  更新其变更记录。

## 14. 完成交付清单

向用户报告完成前，逐项确认：

- 请求的行为已真实实现，而不只是搭好文件；
- 新规则有测试，现有测试没有被削弱；
- 按 §10 选择的检查和 `git diff --check` 通过，说明测试范围及未测项；低风险批次不强制全量；
- 相关场景完成目标屏幕尺寸和真实操作验收；
- 已验证本批影响的声音、触控、键盘、存档、离线能力；没有证据的项目列为未测，不笼统承诺无回归；
- Git 提交按批次完成，工作区状态已说明；
- 没有在缺少对应授权时推送或部署，已授权操作的实际结果已说明；
- 若已部署，已验证线上内容与本地构建一致；
- 最终回复简要列出功能结果、验证结果、提交号、是否部署以及仍存在的限制。

## 15. 文档维护

- 产品范围、技术栈、目录、命令、工作流或硬性约束改变时，更新本文件；
- 线上拓扑、域名、证书、nginx、部署目标或故障经验改变时，更新 `docs/ops.md`；
- 面向使用者的游戏清单和启动方式改变时，更新 `README.md`；
- 文档必须描述当前真实状态，不保留已失效命令作为默认操作；历史事件放入运维变更记录。
